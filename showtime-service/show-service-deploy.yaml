---
# 1. Deployment (Pod 정의 및 RDS Secret 연결)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: showtime-service
  labels:
    app: showtime-service
spec:
  replicas: 2 
  selector:
    matchLabels:
      app: showtime-service
  template:
    metadata:
      labels:
        app: showtime-service
    spec:
      containers:
      - name: showtime-service
        # ⚠️ ECR 이미지 태그를 새로 빌드할 버전으로 업데이트해야 합니다.
        image: 343218219945.dkr.ecr.ap-northeast-2.amazonaws.com/showtime-service:202512150941
        ports:
        - containerPort: 3001 
        
        # ⬇️ DB 접속 정보를 Kubernetes Secret에서 가져오도록 수정 ⬇️
        env:
        - name: DB_HOST
          valueFrom:
            secretKeyRef:
              name: rds-credentials
              key: host
        - name: DB_USER # 💡 코드에서 process.env.DB_USER로 참조됨
          valueFrom:
            secretKeyRef:
              name: rds-credentials
              key: username # ⚠️ rds-credentials Secret은 key가 'username'입니다.
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: rds-credentials
              key: password
        - name: DB_NAME # 💡 DB 이름 환경 변수 추가
          value: "cgv_showtime_db"
---
# 2. Service (내부 로드 밸런싱 정의 - 변경 없음)
apiVersion: v1
kind: Service
metadata:
  name: showtime-service-svc
  labels:
    app: showtime-service
spec:
  selector:
    app: showtime-service
  ports:
    - protocol: TCP
      port: 80 
      targetPort: 3001
  type: ClusterIP