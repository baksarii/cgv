# 3. Gateway API Deployment (DB 연결 필요 없음)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-api
  labels:
    app: gateway-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gateway-api
  template:
    metadata:
      labels:
        app: gateway-api
    spec:
      containers:
      - name: gateway-api
        image: 343218219945.dkr.ecr.ap-northeast-2.amazonaws.com/gateway-api:202512150535
        ports:
        - containerPort: 3000 # 실제 API Gateway가 리스닝하는 포트
        # 💥 Time-out 설정 추가 (300초 = 300000ms)
        env:
        - name: SERVER_SERVLET_ASYNC_TIMEOUT # Spring Boot 환경 변수
          value: "300000"
        - name: HTTP_TIMEOUT # 일반적인 환경 변수 (Node.js 등)
          value: "300000"
        # ⚠️ 참고: 실제 애플리케이션이 사용하는 환경 변수 이름으로 변경해야 할 수 있습니다.
        - name: BOOKING_SERVICE_URL
          value: "http://booking-service-svc"

---
apiVersion: v1
kind: Service
metadata:
  name: gateway-api-svc
  annotations:
    # 💥 NLB 생성을 명시
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb" 
    # 💥 Internal NLB (Private Subnet에 있으므로 필수)
    service.beta.kubernetes.io/aws-load-balancer-scheme: "internet-facing"
    # 💥 TCP Health Check 설정 (503 문제 해결)
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-port: "30664"
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-protocol: "HTTP"
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-path: "/health" # 헬스체크 경로가 있다면
spec:
  type: LoadBalancer # 👈 타입을 LoadBalancer로 변경
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: gateway-api

---
# 5. Ingress (ALB 생성 및 외부 노출) - Time-out 주석이 제거된 깨끗한 버전 사용 권장
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: main-ingress
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing 
    alb.ingress.kubernetes.io/target-type: ip 
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}]'
    # 💥 LBC가 안정화되면 여기에 alb.ingress.kubernetes.io/load-balancer-attributes: idle_timeout.timeout_seconds=300 주석을 다시 추가해야 합니다.
spec:
  ingressClassName: alb # Ingress V1 권장 방식
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: gateway-api-svc
            port:
              number: 80