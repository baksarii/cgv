---
# 1. Deployment (Pod 정의 및 RDS Secret 연결)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service
  labels:
    app: booking-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: booking-service
  template:
    metadata:
      labels:
        app: booking-service
    spec:
      containers:
      - name: booking-service
        image: 343218219945.dkr.ecr.ap-northeast-2.amazonaws.com/booking-service:202512150834
        ports:
        - containerPort: 3002
        
        env:
        - name: DB_HOST
          valueFrom:
            secretKeyRef:
              name: rds-credentials
              key: host
        - name: DB_USERNAME # 💡 코드에서 process.env.DB_USERNAME으로 참조됨
          valueFrom:
            secretKeyRef:
              name: rds-credentials
              key: username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: rds-credentials
              key: password
        - name: DB_NAME # 💡 DB 이름 환경 변수 추가
          value: "cgv_showtime_db"
          
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"

---
# 2. Service (변경 없음)
apiVersion: v1
kind: Service
metadata:
  name: booking-service-svc
  labels:
    app: booking-service
spec:
  selector:
    app: booking-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3002
  type: ClusterIP